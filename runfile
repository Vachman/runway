#!/usr/bin/env bash

$LOGSTASH_CONFIG_FILE=/opt/logstash/logstash.conf

cp ./config/logstash.conf $LOGSTASH_CONFIG_FILE

# Fire up logstash!
exec /opt/logstash/bin/logstash \
    agent \
    --config /opt/ $LOGSTASH_CONFIG_FILE \
    -- \
    web 